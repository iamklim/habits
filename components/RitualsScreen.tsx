import React from "react";
import { Layout, Text } from "@ui-kitten/components";

const RitualsScreen = () => {
  return (
    <Layout style={{ flex: 1, justifyContent: "center" }}>
      <Text category="h1">RITUALS</Text>
    </Layout>
  );
};

export default RitualsScreen;
